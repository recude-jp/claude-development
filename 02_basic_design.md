# 02. 基本設計書

> フェーズ②: 01で確定した目的・スコープを元に、Claudeと対話しながら作成する。
> 「有識者が読んで5分でYes/Noの判断ができる」粒度を目指す。

## メタ情報

| 項目 | 内容 |
|---|---|
| プロジェクト名 | |
| 関連: 要件ブレスト | `docs/process/01_requirements_brainstorm.md` |
| 作成者(初心者) | |
| 作成日 / 版 | |
| レビューステータス | 未レビュー / レビュー中 / 承認 / 差し戻し |

## 1. システム概要

01の「目的」を1段落で要約。

```
(記入)
```

## 1.5. 業務フロー比較(As-Is / To-Be)

01の「現在の業務フロー」のうち、自動化スコープに入った作業について、
自動化後にどう変わるかを対比する。

| # | 作業内容 | As-Is(現状) | To-Be(自動化後) | 人が関わる部分 |
|---|---|---|---|---|
| | | | | |

> 「全部自動化して人が何もしない」ではなく、どこまでをシステムに任せ、
> どこからを人(承認・例外対応等)が担うかを明確にするのが目的。

## 2. 全体構成図

(テキストの簡易図 or 別途画像を `docs/process/diagrams/` に置いてリンク)

```
(構成図 or 説明)
```

## 3. 主要コンポーネント

| コンポーネント | 役割 | 採用技術 | 採用理由(要約) |
|---|---|---|---|
| | | | |

> 採用理由が長くなる/重要な技術選定の場合は、ADR
> (`docs/architecture/ADR-XXXX-*.md`)を別途作成してここからリンクする。

## 4. データ設計

- 主要なデータエンティティ・スキーマ概要
- 永続化方式(DB/ベクトルストア等)

```
(記入)
```

## 5. 処理フロー

主要なユースケースについて、入力→処理→出力の流れを記述。
(シーケンス図的に書けるとベスト)

```
(記入)
```

## 6. インターフェース仕様

- 外部API / 内部API概要
- 入出力フォーマット

```
(記入)
```

## 7. エラーハンドリング・例外方針

```
(記入)
```

## 8. 非機能要件への対応

| 観点 | 方針 |
|---|---|
| 性能 | |
| セキュリティ | |
| 可観測性(ログ/監視) | |
| 拡張性 | |

## 9. 01からの差分・判断結果

01の「未決事項」がどう決まったかをここに反映する。

| 論点(01より) | 決定内容 | 決定理由 |
|---|---|---|
| | | |

## 10. リスク・懸念点(有識者への確認ポイント)

レビューで特に見てほしい箇所を明示する。これが無いと
有識者は全体を一から読む必要があり、レビュー効率が落ちる。

- [ ] 
- [ ] 
